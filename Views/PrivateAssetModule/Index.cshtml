<!DOCTYPE html>
<html>
<head>
    <title>Private Asset Module</title>
</head>
<body>
    <div class="container">
        <h1>Welcome to the Private Asset Module</h1>

        <div class="actions">
            <a href="CreateAsset" class="btn btn-primary">Create a new asset</a>
            <a href="AssetOverview" class="btn btn-secondary">Edit an existing Asset</a>
            <a href="AddTransaction" class="btn btn-primary">Add a Transaction</a>
            <a href="AddTransactionsFile" class="btn btn-primary">Bulk Add Transactions</a>
            <a href="#" class="btn btn-secondary">Edit a transaction</a>
            <a href="AssetViewIndex" class="btn btn-primary">View a summary of the asset to a specific date</a>
            <a href="ValuationSummary" class="btn btn-primary">View a summary of all assets at a specific date</a>
            <a href="ComparePEModelToFund" class="btn btn-primary">Compare a Fund against a Model</a>
        </div>
    </div>

    <style>
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
        }

        .actions {
            margin-top: 20px;
        }

        .btn {
            margin: 10px;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
        }

        .btn-primary {
            background-color: #007bff;
            color: white;
            border: 1px solid #007bff;
        }

        .btn-secondary {
            background-color: white;
            color: #007bff;
            border: 1px solid #007bff;
        }

        .btn:hover {
            opacity: 0.8;
        }
    </style>
</body>
</html>